package com.example.evalutation.infrastructure;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TaskDataModelRepository extends JpaRepository<TaskDataModel, Integer>{
}
